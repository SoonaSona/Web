<style>
  /* ปรับปุ่ม Tab ให้เป็นสีฟ้าทั้งหมด และตัวหนังสือสีขาวเสมอ */
  .tab-btn {
    background-color: #0d6efd; /* สีฟ้าปกติ */
    color: white; /* ตัวหนังสือสีขาว */
    border: 1px solid #0d6efd;
    opacity: 0.6; /* ทำให้จางลงนิดหน่อย (แสดงว่ายังไม่เลือก) */
    transition: 0.2s;
  }

  /* เมื่อเอาเมาส์ชี้ */
  .tab-btn:hover {
    opacity: 0.8;
    color: white;
  }

  /* ปุ่มที่กำลังถูกเลือก (Active) */
  .tab-btn.active-tab {
    background-color: #0b5ed7; /* สีฟ้าเข้ม */
    opacity: 1; /* ชัดเจน 100% */
    box-shadow: 0 0 0 4px rgba(13, 110, 253, 0.2); /* มีแสงวิ้งๆ รอบปุ่ม */
    font-weight: 600;
  }
</style>
<div class="container-fluid p-0">
  <div class="d-flex justify-content-between mb-3 align-items-center">
    <h3 class="fw-bold mb-0">Nurse Station</h3>
  </div>

  <div class="mb-4">
    <button
      class="btn btn-primary tab-btn me-1"
      onclick="changeTab('ผู้ป่วยใน')"
    >
      <i class="bi bi-people-fill"></i> ผู้ป่วยใน
    </button>
    <button
      class="btn btn-outline-primary tab-btn me-1"
      onclick="changeTab('คำสั่งแพทย์')"
    >
      <i class="bi bi-prescription2"></i> คำสั่งแพทย์
    </button>
    <button
      class="btn btn-outline-primary tab-btn me-1"
      onclick="changeTab('Nurses Note')"
    >
      <i class="bi bi-journal-text"></i> บันทึกการพยาบาล
    </button>
    <button
      class="btn btn-outline-primary tab-btn me-1"
      onclick="changeTab('Medication')"
    >
      <i class="bi bi-capsule"></i> Medication
    </button>
    <button
      class="btn btn-outline-primary tab-btn me-1"
      onclick="changeTab('Lab Results')"
    >
      <i class="bi bi-eyedropper"></i> ผล Lab
    </button>
    <button
      class="btn btn-outline-primary tab-btn"
      onclick="changeTab('Consents')"
    >
      <i class="bi bi-file-earmark-check"></i> เอกสารยินยอม
    </button>
  </div>

  <div class="card mb-4 border-0 shadow-sm" style="background-color: #fff">
    <div class="card-body">
      <div class="row g-3">
        <div class="col-md-3">
          <label class="form-label text-muted">Ward</label>
          <select class="form-select">
            <option>consult 1/2</option>
            <option>ICU</option>
            <option>IPD Gen</option>
          </select>
        </div>
        <div class="col-md-4">
          <label class="form-label text-muted">แพทย์เจ้าของคนไข้</label>
          <select class="form-select">
            <option>ทั้งหมด</option>
            <option>นพ. ภูบดี แช่มๆรัมย์</option>
          </select>
        </div>
        <div class="col-md-2 d-flex align-items-end">
          <button class="btn btn-secondary w-100">Clear Filter</button>
        </div>
      </div>
    </div>
  </div>

  <div class="card shadow-sm border-0">
    <div class="card-body p-0">
      <div
        class="p-3 border-bottom d-flex justify-content-between align-items-center bg-light rounded-top"
      >
        <h5 class="mb-0 text-primary">
          <i class="bi bi-table"></i> รายชื่อผู้ป่วย
        </h5>
        <div class="d-flex">
          <input
            id="search"
            class="form-control form-control-sm"
            style="width: 250px"
            placeholder="🔍 ค้นหาชื่อ, HN..."
          />
        </div>
      </div>

      <div class="table-responsive">
        <table class="table table-hover align-middle mb-0">
          <thead class="table-light text-secondary">
            <tr>
              <th class="py-3">No</th>
              <th>เตียง</th>
              <th>HN</th>
              <th>ชื่อ-นามสกุล</th>
              <th>แพทย์เจ้าของไข้</th>
              <th>วัน Admit</th>
              <th>เวลา</th>
              <th>อาหาร</th>
              <th>Lab</th>
              <th>X-ray</th>
            </tr>
          </thead>

          <tbody id="tableBody">
            <tr>
              <td>1</td>
              <td><span class="badge bg-info text-dark">324</span></td>
              <td>6732818224</td>
              <td class="fw-bold text-primary">นายนัททนันท์ หวังตอก</td>
              <td>นายภูบดี แช่มๆรัมย์</td>
              <td>30 ม.ค. 68</td>
              <td>09:09</td>
              <td>
                <button
                  class="btn btn-sm btn-outline-success rounded-pill px-3"
                  onclick="action('อาหาร')"
                >
                  ดู
                </button>
              </td>
              <td>
                <button
                  class="btn btn-sm btn-outline-info rounded-pill px-3"
                  onclick="action('Lab')"
                >
                  ดู
                </button>
              </td>
              <td>
                <button
                  class="btn btn-sm btn-outline-warning rounded-pill px-3"
                  onclick="action('X-ray')"
                >
                  ดู
                </button>
              </td>
            </tr>
            <tr>
              <td>2</td>
              <td><span class="badge bg-info text-dark">325</span></td>
              <td>6732818225</td>
              <td class="fw-bold text-primary">นางสาวใจดี มีสุข</td>
              <td>พญ. สมหญิง จริงใจ</td>
              <td>30 ม.ค. 68</td>
              <td>10:15</td>
              <td>
                <button
                  class="btn btn-sm btn-outline-success rounded-pill px-3"
                  onclick="action('อาหาร')"
                >
                  ดู
                </button>
              </td>
              <td>
                <button
                  class="btn btn-sm btn-outline-info rounded-pill px-3"
                  onclick="action('Lab')"
                >
                  ดู
                </button>
              </td>
              <td>
                <button
                  class="btn btn-sm btn-outline-warning rounded-pill px-3"
                  onclick="action('X-ray')"
                >
                  ดู
                </button>
              </td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>

<div class="modal fade" id="mainModal" tabindex="-1">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <div class="modal-header bg-primary text-white">
        <h5 class="modal-title">รายละเอียด</h5>
        <button
          type="button"
          class="btn-close btn-close-white"
          data-bs-dismiss="modal"
        ></button>
      </div>
      <div class="modal-body text-center p-4" id="modalBody"></div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
          ปิด
        </button>
      </div>
    </div>
  </div>
</div>

<script>
  // เรียกใช้ Bootstrap Modal
  // หมายเหตุ: bootstrap object มีอยู่แล้วเพราะโหลดจาก main.ejs
  const mainModal = new bootstrap.Modal(document.getElementById("mainModal"));

  function changeTab(tab) {
    // ใช้ Backtick (`) เพื่อใส่ตัวแปรลงใน String
    document.getElementById("modalBody").innerHTML = `
        <div class="fs-1 text-primary mb-3"><i class="bi bi-info-circle"></i></div>
        <h4>เปลี่ยนเมนู</h4>
        <p class="text-muted">คุณกำลังเลือกเมนู: <strong>${tab}</strong></p>
    `;
    mainModal.show();

    // Logic เปลี่ยนสีปุ่ม (Optional)
    document.querySelectorAll(".tab-btn").forEach((btn) => {
      btn.classList.remove("btn-primary");
      btn.classList.add("btn-outline-primary");
    });
    event.target.closest("button").classList.remove("btn-outline-primary");
    event.target.closest("button").classList.add("btn-primary");
  }

  function action(type) {
    document.getElementById("modalBody").innerHTML = `
        <div class="fs-1 text-success mb-3"><i class="bi bi-clipboard-data"></i></div>
        <h4>ข้อมูลผู้ป่วย</h4>
        <p>กำลังเปิดข้อมูลส่วน: <strong>${type}</strong></p>
    `;
    mainModal.show();
  }

  // Search Function
  document.getElementById("search").addEventListener("keyup", function () {
    let v = this.value.toLowerCase();
    document.querySelectorAll("#tableBody tr").forEach((row) => {
      row.style.display = row.innerText.toLowerCase().includes(v) ? "" : "none";
    });
  });
</script>
